# Agent Rules

- When providing commands to the user to run in the terminal, **always provide a single, complete command string**. Do not assume the user is in the correct working directory. For example, combine directory changes with the command itself: `cd /path/to/project && your_command_here`.
