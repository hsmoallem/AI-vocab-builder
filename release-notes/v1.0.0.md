# Release v1.0.0 — Initial Public Build

**Date:** June 7, 2026  
**Branch:** `main`  
**Status:** ✅ App builds and runs on Android emulator (Pixel 8)

---

## Features Included

### Core
- **3-tab navigation:** Reader · Daily Phrases · My Words
- **Add word:** Enter any word → AI translates via DeepSeek
- **Multi-meaning translation:** Words with several meanings get separate entries with unique examples
- **German article detection:** `der`/`die`/`das` auto-detected and prepended
- **Sort & search:** Alphabetical or newest-first, live search
- **Swipe-to-delete:** With confirmation dialog

### Flashcards
- Tap to flip (word → translation + examples)
- Swipe left/right to navigate
- Progress bar showing position
- Auto-marks word as reviewed when flipped

### PDF Reader
- **Native file picker** (Android `ACTION_OPEN_DOCUMENT`) — zero external dependencies
- **Two views:** Formatted PDF rendering (via `flutter_pdfview`) + text extraction (via `syncfusion_flutter_pdf`)
- **Tap-to-add:** Select a word in the text view → Add Word dialog opens pre-filled
- **PDF ↔ Text toggle** with segmented button in toolbar
- Compatible with any PDF file

### Daily Phrases
- AI generates 5 practical phrases every day (DeepSeek)
- Tap circle to mark as memorized (visual checkmark + strikethrough)
- Progress counter (`3/5` → trophy when all done)
- Auto-refreshes on new day; old phrases not stored
- Default language: German (configurable)

---

## Tech Stack

| Category | Choice | Why |
|----------|--------|-----|
| **Database** | `sqflite` | Isar unmaintained, incompatible with modern Gradle/AGP |
| **State** | `provider` | Lightweight, no code-gen, Flutter-recommended |
| **AI** | DeepSeek `deepseek-chat` | $0.14/M tokens, JSON-native responses |
| **PDF rendering** | `flutter_pdfview` (Apache 2.0) | 2M+ pub.dev downloads, Android PdfRenderer hardware acceleration |
| **PDF text** | `syncfusion_flutter_pdf` | Best text extraction, `any` constraint avoids version lock |
| **File picker** | Native `MethodChannel` | Eliminated `file_picker` package — source of 100% of Gradle/AGP build failures |
| **Daily phrases** | `shared_preferences` | Today-only data, no DB overhead needed |

---

## Issues We Hit & How We Fixed Them

### 1. Isar → sqflite migration
- **Problem:** Isar 3.1.0 is unmaintained and its generated code is incompatible with Android Gradle Plugin 9.x
- **Fix:** Replaced with `sqflite` — mature SQLite plugin, zero code-gen, works with any AGP version
- **Impact:** Lost Isar's reactive queries but gained build stability

### 2. `file_picker` Gradle/AGP version wars
- **Problem:** `file_picker` compiled against `compileSdk 34` while other plugins required 36. Upgrading to v11 changed API (`FilePicker.platform` → `FilePicker.pickFiles()`). The generated `GeneratedPluginRegistrant.java` kept referencing stale classes even after `flutter clean`.
- **Fix:** Replaced entirely with native Android `Intent.ACTION_OPEN_DOCUMENT` via `MethodChannel`
- **Impact:** Zero external file-picker dependencies; Dart code never changes when adding iOS

### 3. Flutter SDK version mismatch
- **Problem:** Flutter 3.44.1 pre-release caused Dart SDK constraint conflicts with packages expecting stable Dart
- **Fix:** Used `any` constraints on compatible packages, `>=3.5.0 <4.0.0` in pubspec
- **Impact:** Builds on both stable and pre-release Flutter without constraint wars

### 4. PDF library iterations
- Tried `pdfx` (wraps `flutter_pdfview` internally) → unnecessary indirection, not pure Dart as advertised
- Settled on `flutter_pdfview` directly (Apache 2.0, 2M+ pub.dev downloads, uses Android's native PdfRenderer)
- Added `syncfusion_flutter_pdf` for text extraction only, not rendering

### 5. `flutter_pdfview` + `syncfusion_flutter_pdf` name clash
- **Problem:** Both packages export a class called `PdfDocument`
- **Fix:** Import syncfusion with a prefix: `import 'package:syncfusion_flutter_pdf/pdf.dart' as sync;` then reference as `sync.PdfDocument`

### 6. GitHub authentication for server pushes
- **Problem:** Fine-grained PATs didn't work for Git operations; classic tokens expired
- **Fix:** SSH Ed25519 key scoped to this repo only — no expiration, revocable with one click from GitHub Settings

---

## Files Changed

```
lib/
├── config/
│   ├── app_config.dart        # DeepSeek API key, app constants
│   └── theme.dart             # Material 3 light + dark themes
├── models/
│   └── word.dart              # Word data model (10 fields, copyWith, fromMap/toMap)
├── providers/
│   └── word_provider.dart     # ChangeNotifier — CRUD, sort, translate, LoadState enum
├── screens/
│   ├── home_screen.dart       # Tab navigation (Reader / Daily / My Words)
│   ├── pdf_reader_screen.dart # Native file picker + PDF rendering + text extraction + tap-to-add
│   ├── daily_phrases_screen.dart # AI daily phrases with memorized toggle
│   ├── flashcard_screen.dart  # Flip animation, page swiping, progress bar
│   └── word_list_screen.dart  # Search, sort, swipe-to-delete
├── services/
│   ├── database_service.dart  # sqflite CRUD — 6 operations
│   └── translation_service.dart # DeepSeek: multi-meaning translate + daily phrases generation
└── widgets/
    ├── add_word_dialog.dart   # Multi-meaning entry cards, auto-translate, article prepend
    └── word_card.dart         # Display card with review toggle + delete
```

---

## Test Coverage

- **11 unit tests** (`test/word_test.dart`): model constructor, toMap, fromMap, copyWith, roundtrip
- **11 widget tests** (`test/word_card_test.dart`): rendering, callbacks, edge cases
- Run: `flutter test`

---

## Verified On

- ✅ Android emulator (Pixel 8, API 35)
- ✅ Flutter 3.44.1 (stable channel)
- ✅ compileSdk 36, AGP 9.0.1, JDK 17
